class PresentIllnessPrompts_ver2:
    @staticmethod
    def pi_aggravating_factors(texts):
        """PI 악화 요인 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 악화 요인에 대한 내용입니다. 악화 요인에는 아래의 요소가 존재합니다.
            - 딱딱한 음식, 이 악물기, 스트레스, 찬 음식, 단 음식
            
            아래 텍스트를 분석하여 위의 용어가 등장한다면 추출해주세요.
            유사한 단어가 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 콤마로 구분합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_aggravating_factors (악화 요인, 문자열)  
            - 예: 딱딱한 음식

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_aggravating_factors": ""
            }}]
            """

    @staticmethod
    def pi_TMJ_PI_desc(texts):
        """PI 진단/검사 항목 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 진단/검사 항목에 대한 내용입니다. 진단/검사 항목에는 아래의 요소가 존재합니다.
            - 파노라마, Cone Beam CT, 측두하악장애분석검사, 초음파, T-scan, 파노라마(특수), 동기능적교합검사, PCR
            
            아래 텍스트를 분석하여 위의 용어가 등장한다면 추출해주세요.
            유사한 단어가 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 콤마로 구분합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_TMJ_PI_desc (진단/검사 항목, 문자열)  
            - 예: 파노라마, Cone Beam CT

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_TMJ_PI_desc": ""
            }}]
            """

    @staticmethod
    def pi_TMJ_PI_treatment(texts):
        """PI 물리치료 항목 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 물리치료 항목에 대한 내용입니다. 물리치료 항목에는 아래의 요소가 존재합니다.
            - 분사신장치료, 측두하악관절자극요법-단순, 측두하악관절자극요법-전기, 측두하악관절자극요법-복합, 악관절고착해소술, 악관절 고착 해소술, 측두하악관절자극요법-단순자극, 측두하악관절자극요법-전기자극, 측두하악관절자극요법-복합자극, 물리치료, 악관절강세척술
            
            아래 텍스트를 분석하여 위의 용어가 등장한다면 추출해주세요.
            유사한 단어가 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 콤마로 구분합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_TMJ_PI_treatment (물리치료 항목, 문자열)  
            - 예: 분사신장치료, 측두하악관절자극요법-단순

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_TMJ_PI_treatment": ""
            }}]
            """

    @staticmethod
    def pi_drug_treatment(texts):
        """PI 약물치료 항목 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 약물치료 항목에 대한 내용입니다. 
            - 페리슨정(에페리손염산염),소론도정(프레드니솔론),리보트릴정(클로나제팜),세크로정(아세클로페낙),휴모리드정5mg(모사프리드시트르산염수화물),뉴론틴캡슐300밀리그램(가바펜틴),알마겔정,휴모리드정,디푸루칸건조시럽(플루코나졸),리보트릴정,뉴론틴캡슐,세크로정,페리슨정,소론도정,디푸루칸건조시럽,영진멜라토닌서방정,쎄레브렉스캡슐100밀리그램(세레콕시브),아목클정,테그레톨정200밀리그램,뉴론틴캡슐300밀리그램,아목클정625밀리그램,아목클정625밀리그램(아목시실린·클라불란산칼륨),휴온스아목시크라정625밀리그램,알마겔정(알마게이트),살라겐정(필로카르핀염산염),뉴론틴정600밀리그램(가바펜틴),아목사정625밀리그램(아목시실린수화물-클라불란산칼륨),휴모리드정(모사프리드시트르산염수화물),타이레놀8시간이알서방정(아세트아미노펜)
            
            아래 텍스트를 분석하여 약물치료 관련 용어가 등장한다면 추출해주세요.
            약물명이 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 콤마로 구분합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_drug_treatment (약물치료 항목, 문자열)  
            - 예: 소론도정(프레드니솔론), 페리슨정(에페리손염산염)

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_drug_treatment": ""
            }}]
            """

    @staticmethod
    def pi_closing_dentalgear_desc(texts):
        """PI 교합치료 항목 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 교합치료 항목에 대한 내용입니다. 
            - APS,SS,이갈이장치,코골이장치,교합조정,보톡스(교합개선 목적)
            
            아래 텍스트를 분석하여 위의 용어가 등장한다면 추출해주세요.
            유사한 단어가 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 콤마로 구분합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_closing_dentalgear_desc (교합치료 항목, 문자열)  
            - 예: SS, 교합조정

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_closing_dentalgear_desc": ""
            }}]
            """

    @staticmethod
    def pi_PI_check(texts):
        """PI 경과관찰 항목 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 경과관찰 항목에 대한 내용입니다. 
            - 증상 체크 : 증상 ck, 장치 ck, 근육두께 ck, 물리치료, x-ray, 재평가
            - 내원 예정 : 1개월 후, 2주 후, 1주 후
            
            아래 텍스트를 분석하여 경과관찰 관련 용어가 등장한다면 추출해주세요.
            관련 내용이 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 콤마로 구분합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_next_schedule (다음 내원 예정일, 정수형)  
            - 텍스트 내 내원 예정 내용을 추출하여 days 단위로 변환해 기재해 주세요
            - 예: 7

            2. pi_next_ck (다음 내원시 증상 체크, 문자형)  
            - 아래 텍스트를 분석하여 위의 용어가 등장한다면 추출해주세요.
            - 예: 근육두께ck

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_PI_check": ""
            }}]
            """

    @staticmethod
    def pi_PI_diagnosis_jojint(texts):
        """PI 턱관절 진단 내용 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 턱관절 진단 내용에 대한 내용입니다. 
            턱관절 진단에는 K07.65 퇴행성 관절염, K07.63 턱관절 통증, K07.66 저작근 장애 등의 진단명이 포함됩니다.
            
            아래 텍스트를 분석하여 턱관절 진단 관련 용어가 등장한다면 추출해주세요.
            관련 내용이 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 콤마로 구분합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_PI_diagnosis_jojint (턱관절 진단 내용, 문자열)  
            - 예: 퇴행성 관절염 (K07.65), 턱관절 통증 (K07.63)

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_PI_diagnosis_jojint": ""
            }}]
            """

    @staticmethod
    def pi_physical_therapy(texts):
        """PI 저작근 장애 물리치료 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 저작근 장애(K07.66) 물리치료 등에 대한 내용입니다.
            저작근 장애 물리치료에는 아래의 예시가 포함됩니다.
            - 측두하악관절 단순,전기,분사신장치료,복합 자극,복합 자극요법,악관절고착해소술,악관절 고착 해소술,물리치료,초음파 치료

            아래 텍스트를 분석하여 위의 용어가 등장한다면 추출해주세요.
            유사한 단어가 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 콤마로 구분합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_physical_therapy (저작근 장애 물리치료, 문자열)  
            - 예: 측두하악관절 단순,전기,복합 자극,분사신장치료

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_physical_therapy": ""
            }}]
            """

    @staticmethod
    def pi_occlusal_treatment(texts):
        """PI 교합안정장치 등 장치치료 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 교합안정장치(Splint) 등 장치치료에 대한 내용입니다.
            장치치료에는 APS, SS 장치 장착 및 조정, 교합 조정, 장치 제작 등이 포함됩니다.
            
            아래 텍스트를 분석하여 교합안정장치 등 장치치료 관련 용어가 등장한다면 추출해주세요.
            관련 내용이 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 하나의 문자열로 요약해주세요.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_occlusal_treatment (교합안정장치 등 장치치료, 문자열)  
            - 예: APS, SS 장치 장착 및 조정

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_occlusal_treatment": ""
            }}]
            """

    @staticmethod
    def pi_medication_prescription(texts):
        """PI 약물 처방 상세 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 약물 처방 상세에 대한 내용입니다.
            약물 처방에는 "페리슨정(에페리손염산염) 1/1회/14일 :: 취침 직전 복용" 처럼, 복용 용법/횟수/기간 등의 상세 정보가 포함됩니다.
            
            아래 텍스트를 분석하여 약물 처방 상세 관련 용어가 등장한다면 추출해주세요.
            관련 내용이 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 문장으로 나열해주세요.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_medication_prescription (약물 처방 상세, 문자열)  
            - 예: 페리슨정(에페리손염산염) - 1/1회/14일, 소론도정(프레드니솔론) - 1/1회/14일

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_medication_prescription": ""
            }}]
            """

    @staticmethod
    def pi_other_treatment(texts):
        """PI 그 외 치료 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 그 외 보톡스 시술, 악관절 강세척술, 교합조정, 발치 등에 대한 내용입니다.
            그 외 치료에는 보톡스 시술, 악관절 강세척술, 교합조정, 발치 등이 포함될 수 있습니다.
            
            아래 텍스트를 분석하여 그 외 치료 관련 용어가 등장한다면 추출해주세요.
            관련 내용이 없다면 빈 문자열로 추출합니다.
            여러가지가 등장한다면 하나의 문자열로 요약해주세요.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_other_treatment (그 외 치료, 문자열)  
            - 예: 보톡스 시술, 악관절 강세척술

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_other_treatment": ""
            }}]
            """

    @staticmethod
    def pi_onset(texts):
        """PI 발현 시기 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 발현 시기에 대한 내용입니다.
            발현 시기는 "3개월 전", "2023년 1월", "발병 시기 미상" 등으로 표현될 수 있습니다.
            
            아래 텍스트를 분석하여 발현 시기 관련 표현이 등장한다면 추출해주세요.
            텍스트에서 구체적으로 언급된 경우만 추출하고, 없으면 빈 문자열로 추출합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_onset (발현 시기, 문자열)  
            - 예: 3개월 전

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_onset": ""
            }}]
            """

    @staticmethod
    def pi_pattern(texts):
        """PI 증상 양상 프롬프트"""
        return f"""
            다음은 본 문서에 등장하는 증상 양상에 대한 내용입니다.
            증상 양상은 "constant" (지속성), "intermittent" (간헐성), "progressive" (점진적 악화) 등으로 표현될 수 있습니다.
            
            아래 텍스트를 분석하여 증상 양상 관련 표현이 등장한다면 추출해주세요.
            명확히 언급된 경우에만 지정하고, 없으면 빈 문자열로 추출합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_pattern (증상 양상, 문자열)  
            - 예: constant, intermittent, progressive

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_pattern": ""
            }}]
            """

    @staticmethod
    def pi_status(texts):
        """PI 현재 상태 프롬프트"""
        return f"""
            중요: 텍스트에서 직접적으로 언급된 정보만 추출하세요. 추측이나 유추는 하지 마세요. 텍스트에 명확히 언급되지 않은 정보는 반드시 빈 문자열("")로 응답하세요.
            
            다음은 본 문서에 등장하는 현재 상태에 대한 내용입니다.
            현재 상태는 "improving" (호전), "unchanged" (변화 없음), "worsening" (악화) 등으로 표현될 수 있습니다.
            
            아래 텍스트를 분석하여 현재 상태 관련 표현이 등장한다면 추출해주세요.
            명시되지 않으면 빈 문자열로 추출합니다.
            다음 필드를 JSON 형식으로 추출해주세요:
            
            1. pi_status (현재 상태, 문자열)  
            - 예: improving, unchanged, worsening

            텍스트 목록:
            {texts}

            다음 JSON 형식으로 응답해주세요:
            [{{
                "pi_status": ""
            }}]
            """